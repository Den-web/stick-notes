import React from 'react';
import TextArea from '../atoms/TextArea';

const NoteTitle = () => {
  return (
    <label>
      Sticky Note Content:
      <TextArea />
    </label>
  );
};

export default NoteTitle;